pre-commit:
  commands:
    lint-staged:
      run: bunx lint-staged

pre-push:
  commands:
    typecheck:
      run: bun run typecheck
    build:
      run: bun run build
    wrangler-dry-run:
      run: cd apps/api && bunx wrangler deploy --dry-run
